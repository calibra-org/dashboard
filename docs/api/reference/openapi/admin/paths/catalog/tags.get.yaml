operationId: adminTagsIndex
summary: List tags (admin)
description: >
    Paginated list of every tag on the unified TableView grammar.

    **TableView filterable fields**: `id`, `menu_order`, `created_at`.

    **TableView orderable fields**: `id`, `slug`, `menu_order`, `created_at`, `used_count`.

    `slug` lives on the translations table, so it is sort-only (ordered by the per-row translated
    slug via a correlated subquery); free-text slug matching is available through `?q=`.

    `used_count` sorts by the live `product_tag_links` count via a correlated subquery.
    Default page size is 100.
tags: [Admin / Catalog / Tags]

parameters:
    - $ref: "../../../common/components/parameters/table-view/Page.yaml"
    - $ref: "../../../common/components/parameters/table-view/Limit500.yaml"
    - $ref: "../../../common/components/parameters/table-view/Filter.yaml"
    - $ref: "../../../common/components/parameters/table-view/FilterOr.yaml"
    - $ref: "../../../common/components/parameters/table-view/Sort.yaml"
    - $ref: "../../../common/components/parameters/LocaleHeader.yaml"
    - { name: q, in: query, schema: { type: string, maxLength: 120 }, description: "Multi-column ILIKE across slug + translated name." }

responses:
    "200":
        description: Paginated tag list.
        content:
            application/json:
                schema:
                    type: object
                    required: [data, meta]
                    properties:
                        data:
                            type: array
                            items: { $ref: "../../components/schemas/AdminTaxonomy.yaml" }
                        meta: { $ref: "../../../common/components/schemas/PaginationMeta.yaml" }
    "401": { $ref: "../../../common/components/responses/Unauthorized.yaml" }
    "403": { $ref: "../../../common/components/responses/Forbidden.yaml" }
