type: http
scheme: bearer
bearerFormat: opaque
description: >
    Opaque session token issued by `POST /api/v1/auth/login` (storefront) or the admin login
    endpoint. Pass it as `Authorization: Bearer <token>`. Tokens are scoped to the user that
    issued them; admin endpoints additionally require the user to have admin privileges and
    return 403 otherwise.
