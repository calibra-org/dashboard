operationId: catalogTagsIndex
summary: List product tags (storefront)
description: Returns every product tag with localised `name`, `slug`, and `description`. Sorted by `menu_order` ascending.
tags: [Catalog]
security: []

parameters:
    - $ref: "../../../common/components/parameters/LocaleHeader.yaml"

responses:
    "200":
        description: Tag list.
        content:
            application/json:
                schema:
                    type: object
                    required: [data]
                    properties:
                        data:
                            type: array
                            items: { $ref: "../../components/schemas/Tag.yaml" }
