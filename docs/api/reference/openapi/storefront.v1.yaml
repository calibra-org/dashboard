openapi: 3.1.0

info:
    title: Calibra Storefront API
    version: "1.0.0"
    description: >
        Public REST API for the Calibra commerce backend — storefront-facing endpoints consumed by
        `apps/web/`. Persian (`fa`) is the default locale; pass `Accept-Language: en` to receive
        English copy. Money is reported in Rial minor units (`BIGINT`). Translatable content
        (product names, category names, descriptions) is resolved server-side from the active
        locale.

        ## Conventions

        - **Pagination.** Paginated list endpoints take `page` and `perPage` query params and
          return `{ data: [...], meta: { page, perPage, total, lastPage } }`.
        - **Money.** Every monetary field is in Rial minor units (BIGINT). The display currency
          (`IRR` or `IRT`) is reported separately on orders.
        - **Locale.** The active locale comes from `Accept-Language` (defaults to `fa`).
          Server-resolved strings (product names, region names, error messages) come back in that
          locale.
        - **Auth.** Public endpoints carry `security: []`. Authenticated endpoints use a bearer
          token from `POST /api/v1/auth/login`.
        - **Idempotency.** `POST /checkout/submit` and `POST /payment/init` accept an
          `Idempotency-Key` header so retries don't re-run side effects.
        - **HEAD methods.** AdonisJS auto-registers a `HEAD` handler for every `GET` route. Each
          `head:` entry below is a documentation companion that returns the same headers as the
          paired `get:` operation with an empty body.
    contact:
        name: Calibra Backend Team
        url: https://calibra.dev/support
        email: support@calibra.dev
    license:
        name: "UNLICENSED (Proprietary)"

servers:
    - url: http://localhost:3333
      description: Local development
    - url: https://api.calibra.dev
      description: Production

tags:
    - name: Auth
      description: Registration, login, logout, password reset.
    - name: Catalog
      description: Read-only product, category, attribute, brand, tag, and review endpoints.
    - name: Cart
      description: >
          Server-side cart resolved per visitor — anonymous via the `cart_token` cookie, or by
          bearer token once the customer signs in.
    - name: Checkout
      description: Cart → draft order → submit, with `Idempotency-Key` dedup on submit.
    - name: Account
      description: Authenticated customer profile, addresses, downloads, and order history.
    - name: Payment
      description: PSP init + gateway callback endpoints (storefront-facing).
    - name: Factor Payment
      description: Public invoice and proforma payment-link operations.
    - name: Content
      description: Published tenant content and privacy-conscious engagement events.
    - name: SEO
      description: Public tenant SEO metadata, robots, sitemap, structured data, and redirects.
    - name: Currency
      description: Public display-currency configuration for the money formatter.
    - name: Tenant
      description: Public tenant profile + runtime branding for the resolved shop.

components:
    securitySchemes:
        BearerAuth:
            $ref: "./common/components/securitySchemes/BearerAuth.yaml"

security: []

paths:
    /api/v1/auth/register:
        post:
            $ref: "./storefront/paths/auth/register.post.yaml"
    /api/v1/auth/login:
        post:
            $ref: "./storefront/paths/auth/login.post.yaml"
    /api/v1/auth/otp/request:
        post:
            $ref: "./storefront/paths/auth/otp-request.post.yaml"
    /api/v1/auth/otp/verify:
        post:
            $ref: "./storefront/paths/auth/otp-verify.post.yaml"
    /api/v1/auth/impersonation/stop:
        post:
            $ref: "./storefront/paths/auth/impersonation-stop.post.yaml"
    /api/v1/auth/logout:
        post:
            $ref: "./storefront/paths/auth/logout.post.yaml"
    /api/v1/auth/password/forgot:
        post:
            $ref: "./storefront/paths/auth/password-forgot.post.yaml"
    /api/v1/auth/password/reset:
        post:
            $ref: "./storefront/paths/auth/password-reset.post.yaml"
    /api/v1/auth/me:
        get:
            $ref: "./storefront/paths/auth/me.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"

    /api/v1/currency:
        get:
            $ref: "./storefront/paths/currency/currency.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"

    /api/v1/storefront/tenant:
        get:
            $ref: "./storefront/paths/tenant/tenant.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"

    /api/v1/products:
        get:
            $ref: "./storefront/paths/catalog/products.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/products/{slug}:
        get:
            $ref: "./storefront/paths/catalog/products-slug.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/products/{id}/variations:
        get:
            $ref: "./storefront/paths/catalog/products-id-variations.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/products/{id}/reviews:
        get:
            $ref: "./storefront/paths/catalog/products-id-reviews.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
        post:
            $ref: "./storefront/paths/catalog/products-id-reviews.post.yaml"
    /api/v1/categories:
        get:
            $ref: "./storefront/paths/catalog/categories.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/categories/{slug}:
        get:
            $ref: "./storefront/paths/catalog/categories-slug.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/attributes:
        get:
            $ref: "./storefront/paths/catalog/attributes.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/attributes/{id}/terms:
        get:
            $ref: "./storefront/paths/catalog/attributes-id-terms.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/brands:
        get:
            $ref: "./storefront/paths/catalog/brands.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/tags:
        get:
            $ref: "./storefront/paths/catalog/tags.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"

    /api/v1/cart:
        get:
            $ref: "./storefront/paths/cart/cart.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/cart/items:
        post:
            $ref: "./storefront/paths/cart/items.post.yaml"
        delete:
            $ref: "./storefront/paths/cart/items.delete.yaml"
    /api/v1/cart/items/{line_id}:
        patch:
            $ref: "./storefront/paths/cart/items-line_id.patch.yaml"
        delete:
            $ref: "./storefront/paths/cart/items-line_id.delete.yaml"
    /api/v1/cart/customer:
        post:
            $ref: "./storefront/paths/cart/customer.post.yaml"
    /api/v1/cart/shipping-rate:
        post:
            $ref: "./storefront/paths/cart/shipping-rate.post.yaml"
    /api/v1/cart/coupons:
        post:
            $ref: "./storefront/paths/cart/coupons.post.yaml"
    /api/v1/cart/coupons/{code}:
        delete:
            $ref: "./storefront/paths/cart/coupons-code.delete.yaml"

    /api/v1/checkout:
        get:
            $ref: "./storefront/paths/checkout/checkout.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
        put:
            $ref: "./storefront/paths/checkout/checkout.put.yaml"
    /api/v1/checkout/submit:
        post:
            $ref: "./storefront/paths/checkout/submit.post.yaml"
    /api/v1/checkout/orders/{order_key}/pay:
        post:
            $ref: "./storefront/paths/checkout/checkout-orders-key-pay.post.yaml"

    /api/v1/account/me:
        get:
            $ref: "./storefront/paths/account/me.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
        put:
            $ref: "./storefront/paths/account/me.put.yaml"
    /api/v1/account/orders:
        get:
            $ref: "./storefront/paths/account/orders.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/account/orders/{id}:
        get:
            $ref: "./storefront/paths/account/orders-id.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/account/orders/{id}/history:
        get:
            $ref: "./storefront/paths/account/orders-id-history.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/account/orders/{id}/notes:
        get:
            $ref: "./storefront/paths/account/orders-id-notes.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/account/addresses:
        get:
            $ref: "./storefront/paths/account/addresses.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
        post:
            $ref: "./storefront/paths/account/addresses.post.yaml"
    /api/v1/account/addresses/{id}:
        get:
            $ref: "./storefront/paths/account/addresses-id.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
        patch:
            $ref: "./storefront/paths/account/addresses-id.patch.yaml"
        delete:
            $ref: "./storefront/paths/account/addresses-id.delete.yaml"
    /api/v1/account/downloads:
        get:
            $ref: "./storefront/paths/account/downloads.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/account/downloads/{id}/url:
        get:
            $ref: "./storefront/paths/account/downloads-id-url.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"

    /api/v1/payment/init:
        post:
            $ref: "./storefront/paths/payment/init.post.yaml"
    /api/v1/payment/callback/{gateway_code}:
        get:
            $ref: "./storefront/paths/payment/callback-gateway_code.get.yaml"
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
        post:
            $ref: "./storefront/paths/payment/callback-gateway_code.post.yaml"

    /api/v1/factor/pay/{code}:
        parameters:
            - name: code
              in: path
              required: true
              schema: { type: string, minLength: 16, maxLength: 32 }
        get:
            operationId: factorPublicPaymentShow
            summary: Show the public payment payload for an invoice or proforma
            tags: [Factor Payment]
            security: []
            responses:
                "200":
                    description: Public factor document, outstanding amount, payment instructions, and available gateways.
                    content:
                        application/json:
                            schema: { $ref: "./common/components/schemas/FactorPublicPaymentEnvelope.yaml" }
                "404": { description: Payment link not found. }
                "410": { description: Payment link is inactive or expired. }
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/factor/pay/{code}/init:
        parameters:
            - name: code
              in: path
              required: true
              schema: { type: string, minLength: 16, maxLength: 32 }
        post:
            operationId: factorPublicPaymentInit
            summary: Initialize payment for an invoice or proforma
            tags: [Factor Payment]
            security: []
            parameters:
                - name: Idempotency-Key
                  in: header
                  required: true
                  schema: { type: string, minLength: 16, maxLength: 191 }
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            type: object
                            properties:
                                gateway_id: { type: integer, minimum: 1 }
                            additionalProperties: false
            responses:
                "200":
                    description: Gateway attempt initialized.
                    content:
                        application/json:
                            schema: { $ref: "./common/components/schemas/FactorPaymentInitEnvelope.yaml" }
                "409": { description: Document is already paid or cannot accept payment. }
                "410": { description: Payment link is inactive or expired. }
                "422": { description: Gateway selection is invalid. }

    /api/v1/content/posts:
        get:
            operationId: publicContentPostsIndex
            summary: List published indexable content
            tags: [Content]
            security: []
            parameters:
                - { name: page, in: query, schema: { type: integer, minimum: 1 } }
                - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 50 } }
                - { name: type, in: query, schema: { type: string, enum: [article, news, guide, case_study, landing] } }
                - { name: category, in: query, schema: { type: string, maxLength: 191 } }
                - { name: q, in: query, schema: { type: string, maxLength: 180 } }
                - { name: locale, in: query, schema: { type: string, enum: [fa, en] } }
            responses:
                "200": { description: Paginated published content, content: { application/json: { schema: { $ref: "./common/components/schemas/ContentPostListEnvelope.yaml" } } } }
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/content/posts/{slug}:
        parameters:
            - { name: slug, in: path, required: true, schema: { type: string, maxLength: 191 } }
            - { name: locale, in: query, schema: { type: string, enum: [fa, en], default: fa } }
        get:
            operationId: publicContentPostsShow
            summary: Show one published content item with categories, tags, and published products
            tags: [Content]
            security: []
            responses:
                "200": { description: Published content, content: { application/json: { schema: { $ref: "./common/components/schemas/ContentPostEnvelope.yaml" } } } }
                "404": { description: Content not found. }
                "422": { description: Unsupported locale. }
        head:
            $ref: "./common/components/operations/HeadCompanion.yaml"
    /api/v1/content/events:
        post:
            operationId: publicContentEvent
            summary: Record a rate-limited content view, product click, or add-to-cart assist event
            tags: [Content]
            security: []
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            type: object
                            additionalProperties: false
                            required: [post_id, event_type]
                            properties:
                                post_id: { type: integer, format: int64 }
                                product_id: { type: [integer, "null"], format: int64 }
                                event_type: { type: string, enum: [view, product_click, add_to_cart] }
                                session_key: { type: [string, "null"], maxLength: 80 }
                                metadata: { type: object, additionalProperties: true }
            responses:
                "202": { description: Event accepted or deduplicated. }
                "422": { description: Invalid event or unlinked product. }
                "429": { description: Rate limit exceeded. }

    /api/v1/seo/robots:
        get: { operationId: seoRobots, summary: Render tenant robots.txt, tags: [SEO], responses: { "200": { description: Plain-text robots document. } } }
        head: { $ref: "./common/components/operations/HeadCompanion.yaml" }
    /api/v1/seo/sitemap.xml:
        get: { operationId: seoSitemap, summary: Render tenant XML sitemap, tags: [SEO], responses: { "200": { description: XML sitemap. } } }
        head: { $ref: "./common/components/operations/HeadCompanion.yaml" }
    /api/v1/seo/organization:
        get: { operationId: seoOrganization, summary: Read tenant Organization schema, tags: [SEO], responses: { "200": { description: Organization JSON-LD. } } }
        head: { $ref: "./common/components/operations/HeadCompanion.yaml" }
    /api/v1/seo/entity/{kind}/{id}:
        parameters:
            - { name: kind, in: path, required: true, schema: { type: string } }
            - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
        get: { operationId: seoEntity, summary: Read public SEO metadata for one entity, tags: [SEO], responses: { "200": { description: Public SEO metadata. }, "404": { description: Entity not found. } } }
        head: { $ref: "./common/components/operations/HeadCompanion.yaml" }
    /api/v1/seo/redirect:
        get:
            operationId: seoRedirectResolve
            summary: Resolve one tenant redirect path
            tags: [SEO]
            parameters: [{ name: path, in: query, required: true, schema: { type: string } }]
            responses: { "200": { description: Redirect target or null. } }
        head: { $ref: "./common/components/operations/HeadCompanion.yaml" }
