"use client";

/** Convenience alias for {@link PopoverRoot} — matches the shadcn-style API existing consumers expect. */
export {
    PopoverClose,
    PopoverContent,
    type PopoverContentProps,
    PopoverRoot,
    PopoverRoot as Popover,
    PopoverTrigger,
} from "./popover.parts";
