# `HoverCard`

Tier-2 hover-reveal floating card. Wraps Base UI's `PreviewCard`. Used for inline previews — full image thumbnail on hover, full tag list when a row only shows the first few, etc. **Not** for primary content; use `Popover` (click) for anything keyboard-essential.
