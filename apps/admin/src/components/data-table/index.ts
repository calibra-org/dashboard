/**
 * Backwards-compat shim. The data-grid primitive moved from `components/data-table/` to
 * `components/ui/data-grid/` in prompt 04. Removed once any straggler call sites move over.
 */
export * from "../ui/data-grid";
