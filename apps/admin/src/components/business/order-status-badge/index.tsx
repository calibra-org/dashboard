/**
 * Tier-4 OrderStatusBadge. Maps the `OrderStatus` enum to a `StatusBadge` tone. Re-exports the
 * existing flat-file primitive for the canonical tier-4 import path.
 */
export { OrderStatusBadge } from "#/components/OrderStatusBadge";
