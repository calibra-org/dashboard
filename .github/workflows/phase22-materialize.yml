name: Phase 22 Carrier Archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Phase 22 source was validated and merged through clean PR #68. This carrier is archived and must not publish source."
