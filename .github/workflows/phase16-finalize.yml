name: Phase 16 Carrier Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - name: Carrier status
        run: echo "Phase 16 finalized source was merged through PR #66; this carrier is archived."
